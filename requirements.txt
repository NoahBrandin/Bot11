# Core async HTTP/WS clients -- Binance/Polymarket REST polling (aiohttp) and
# streaming (websockets: Binance klines, Polymarket order book).
aiohttp>=3.9
websockets>=12.0

# .env loading (orchestrator.py, performance_review.py).
python-dotenv>=1.0

# Polymarket's official SDK (import name `polymarket`) -- AsyncSecureClient
# for live order placement/balances (execution/live.py), AsyncPublicClient
# for read-only wallet queries (performance_review.py) and for the Chainlink
# TWAP price stream (datastream/chainlink_feed.py, needs >=0.3.0 for
# polymarket.streams). See server_src/execution/live.py's module docstring
# for why this SDK was chosen over py_clob_client_v2.
polymarket-client>=0.3.0

# Equity-curve/drawdown/PnL-distribution chart in performance_review.py.
matplotlib>=3.8

# Vectorized time-series ops (research/) and statistical tests (KS test,
# t-test) in research/event_study.py.
numpy>=2.0
scipy>=1.13
